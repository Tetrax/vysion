# Next up

- Move the CVE check off HTML scraping and onto the Fortinet PSIRT API
- Add a fixture config and tests around the parsing functions
- Decide whether the deployed app needs authentication
- Retire the dead code: `legacy_adapter.py`, `config.py` legacy settings, `fix_imports.py`, the unused `msal` import
